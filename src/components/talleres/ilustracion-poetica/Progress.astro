---
interface Props {
	steps: { stepNumber: string | number; stepTitle: string }[];
}

const { steps } = Astro.props;
// TODO: Make progress pills clickable
// TODO: Make progress bar sticky
---

<ul class="progress">
        {steps.map((step, index) => (
            <li class="step-pill" class:list={{ "is-active": index === 0 }} data-step-pill>
                <span class="step-number">{step.stepNumber}. </span> 
                <span class="step-title">{step.stepTitle}</span>
            </li>
        ))}
</ul>

<style>
    .progress {
        display: flex;
        justify-content: space-between;
        margin: 0;
    }

    .step-pill {
        background-color: var(--color-accent-light);
        border-radius: var(--space-half);
        border: 1px solid var(--color-black);
        color: var(--color-black);
        height: var(--space);
        margin: 0;
        text-align: center;
        text-wrap: nowrap;
        width: 32%;
        opacity: var(--opacity-medium);
    }

    .step-pill.is-active {
        opacity: var(--opacity-full);
        border-color: var(--color-accent);

        .step-title,
        .step-number {
            opacity: var(--opacity-full);
        }
    }

    .step-pill.is-active {
        color: var(--color-accent-light);
        background-color: var(--color-accent);
    }

    .step-pill.is-completed {
        background-color: var(--color-black);
        color: var(--color-accent-light);
        opacity: var(--opacity-medium);

        .step-title,
        .step-number {
            opacity: var(--opacity-full);
        }
    }

    .step-number {
        display: none;
    }

    @media screen and (min-width: 400px) {
        .step-pill {
            padding: 0 var(--space-half);
        }

        .step-number {
            display: inline;
        }
    }
</style>